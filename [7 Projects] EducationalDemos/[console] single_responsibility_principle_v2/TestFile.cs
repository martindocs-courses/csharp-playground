// Need to have C#10 and newer
//var stopwatch = Stopwatch.StartNew();

//for (int i = 0; i < 1000;  i++){
//    Console.WriteLine($"Loop number {i}" );
//}

//stopwatch.Stop();
//Console.WriteLine("Time in ms" + stopwatch.ElapsedMilliseconds);
