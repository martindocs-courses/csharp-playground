<Window x:Class="CustomUserControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomUserControl"
        xmlns:UserControl="clr-namespace:CustomUserControl.View.UserControler"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition />
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <UserControl:MenuBar />
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15*" />
                <ColumnDefinition Width="65*" />
                <ColumnDefinition Width="20*" />
            </Grid.ColumnDefinitions>

            <Rectangle Fill="Bisque" />
            <TextBlock Name="exit_menu" Grid.Column="1"  Text="Context" VerticalAlignment="Center"  HorizontalAlignment="Center" FontSize="36"/>
            <Rectangle Grid.Column="2" Fill="Bisque" />
        </Grid>
        
        <Rectangle Grid.Row="2" Fill="Beige" />
        
    </Grid>
</Window>
